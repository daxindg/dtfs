- defaults:
    link:
        glob: true
        relink: true
        force: true

- create:
    - ~/.config/nvim

- link:
    ~/.config/nvim/: nvim/*
    ~/.config/opencode/opencode.jsonc: opencode.jsonc
    ~/.tmux.conf: .tmux.conf
    ~/.vimrc: .vimrc
    ~/.zshrc: .zshrc

